## 1. Backend loader — accept label as string-or-mapping (Prompt action templates loaded from registry/actions.yaml)

- [x] 1.1 Extend `ActionTemplate` in `app/services/actions.py` so that **prompt action templates loaded from registry/actions.yaml** expose a `labels: Mapping[str, str]` field alongside the existing `label: str`, with `labels` always non-empty and `label` set to `labels["en"]` when present else the first inserted value — Allow `label` as a string-or-mapping union, normalize to mapping at load. Verify with a new pytest case `tests/test_actions.py::test_action_template_exposes_labels_mapping` asserting both fields are populated for a string-label fixture (`labels == {"en": value}`, `label == value`).
- [x] 1.2 [P] Normalize a YAML `label: "..."` shorthand to `{"en": "..."}` in the loader so existing single-language registries load with no change to operator-visible behavior — String label normalizes to en-only mapping. Verify with pytest `tests/test_actions.py::test_string_label_normalizes_to_en_mapping` covering an entry that uses only the string form.
- [x] 1.3 [P] Accept a YAML `label: {en: "...", "zh-TW": "..."}` mapping verbatim in the loader so bilingual chips ship without code changes — Bilingual mapping label loaded verbatim. Verify with pytest `tests/test_actions.py::test_mapping_label_loaded_verbatim` covering an `en`+`zh-TW` entry and an `en`-only entry, asserting `labels` and `label` per the example table in the spec.

## 2. Backend loader — strict validation on mapping form (Prompt action templates loaded from registry/actions.yaml)

- [x] 2.1 Raise `ActionRegistryError` naming the action id when `label: {}` is encountered, so the server refuses to start rather than serving a chip with no usable text — Loader validates locale-mapping shape strictly. Verify with pytest `tests/test_actions.py::test_empty_label_mapping_refuses_startup` asserting the raised error message contains the offending id.
- [x] 2.2 [P] Raise `ActionRegistryError` naming the action id AND the offending locale key when a label mapping contains a non-string value (e.g., `{en: 42}`) — Loader validates locale-mapping shape strictly. Verify with pytest `tests/test_actions.py::test_non_string_label_value_refuses_startup` asserting both id and locale key appear in the error message.
- [x] 2.3 [P] Raise `ActionRegistryError` naming the action id and the actual type when `label` is neither a string nor a mapping (e.g., `[]`, `42`, `null`) — Loader validates locale-mapping shape strictly. Verify with pytest `tests/test_actions.py::test_invalid_label_type_refuses_startup` parameterized over list, int, and null inputs.
- [x] 2.4 [P] Accept unknown locale keys (any string outside `AVAILABLE_LOCALES`) without raising, passing them through to the resulting `labels` mapping — Loader validates locale-mapping shape strictly (forward-compat clause). Verify with pytest `tests/test_actions.py::test_unknown_locale_key_accepted` covering `label: {fr: "Envoyer"}` and asserting `labels["fr"] == "Envoyer"` plus no error.

## 3. API — extend GET /actions endpoint exposes loaded templates

- [x] 3.1 Extend the GET /actions endpoint exposes loaded templates contract by including a `labels` mapping per entry in the JSON body alongside the existing `label` string, so PWA clients receive the canonical localized label set without losing the legacy field — `GET /actions` returns BOTH `label` (string) and `labels` (mapping) per entry. Verify with pytest `tests/test_actions.py::test_actions_endpoint_includes_labels_field` asserting every entry has both fields and `labels` is a non-empty dict.
- [x] 3.2 [P] Document that the endpoint SHALL NOT perform `Accept-Language` negotiation: two requests with different `Accept-Language` headers return byte-identical bodies — Same response regardless of Accept-Language. Verify with pytest `tests/test_actions.py::test_actions_endpoint_ignores_accept_language` issuing two requests with different headers and asserting `response.content` is identical bytes.

## 4. Registry — reseed with seven bilingual actions (Prompt action templates loaded from registry/actions.yaml)

- [x] 4.1 Rewrite `registry/actions.yaml` to ship exactly seven actions in this order — `passthrough`, `cleanup-light`, `punctuate`, `polish`, `meeting-notes`, `translate-en`, `formalize` — each with bilingual `labels` (`en` + `zh-TW`) and templates following the owner-supplied prompt content — Retire `summarize`, introduce `meeting-notes` with a new id (not a rename). Verify with pytest `tests/test_actions.py::test_shipped_registry_contains_seven_actions_in_order` asserting `[a.id for a in load_actions(DEFAULT_REGISTRY_PATH)]` equals the exact seven-id list above, and `labels` contains both `en` and `zh-TW` for every entry.
- [x] 4.2 [P] Adapt the meeting-notes chief-of-staff template so it consumes only `{transcript}` and instructs the LLM to infer attendees/date from the transcript or write `[NEEDS CLARIFICATION]` instead of relying on the source template's `{meeting_type}`, `{attendees}`, `{date}` placeholders — Adapt the meeting-notes source template to `{transcript}` only. Verify with pytest `tests/test_actions.py::test_meeting_notes_template_uses_only_transcript_placeholder` asserting `"{transcript}"` appears at least once in the template AND none of `"{meeting_type}"`, `"{attendees}"`, `"{date}"` appear.

## 5. Frontend chip bar — locale-aware label resolution (PWA chip bar renders the active-locale label with deterministic fallback)

- [x] 5.1 Update `ActionTemplate` interface in `frontend/src/ui/actions-bar.ts` to add optional `labels?: Record<string, string>`, and replace `chip.textContent = action.label` with a resolver that walks `labels[active] → labels.en → first labels entry → label → id` — `ActionsBar` resolves label with `labels[active] → labels.en → first mapping entry → label string → action.id`. Verify with vitest `frontend/src/ui/actions-and-settings.test.ts::"renders chip text from labels[activeLocale]"` asserting chip text matches the active-locale label when both `en` and `zh-TW` are present.
- [x] 5.2 [P] Cover the fallback chain in vitest by adding cases for: labels has only `en` while active locale is `zh-TW` (renders `en`), labels has only `zh-TW` while active locale is `en` (renders `zh-TW` — first mapping entry), `labels` field absent (renders `label` string), no usable label anywhere (renders action `id`) — PWA chip bar renders the active-locale label with deterministic fallback. Verify by extending `frontend/src/ui/actions-and-settings.test.ts` with four new test cases mirroring the fallback example table from the spec; `cd frontend && bun run test` passes.
- [x] 5.3 [P] Keep the `actions.passthroughLabel` i18n string in `frontend/src/i18n/strings.ts` and confirm the empty-registry fallback still renders one chip with the localized passthrough label — PWA chip bar renders the active-locale label with deterministic fallback (empty-registry branch). Verify with vitest `frontend/src/ui/actions-and-settings.test.ts::"fallback passthrough chip uses i18n string"` asserting chip text equals `t("actions.passthroughLabel")` after a 404 from `fetchActions`.

## 6. Verification

- [x] 6.1 Run `uv run pytest tests/test_actions.py -v` and confirm every test from sections 1–4 passes — full pytest action-registry suite is green.
- [x] 6.2 [P] Run `cd frontend && bun run test src/ui/actions-and-settings.test.ts` and confirm every test from section 5 passes — vitest action-bar suite is green.
- [x] 6.3 [P] Run `make lint` and confirm no new warnings introduced by the loader changes — lint is clean.
- [x] 6.4 Manual smoke: start the server with the new YAML, `curl -s $HOST/actions | jq '.actions | length'` returns `7` and `jq '.actions[].labels | keys'` returns `["en","zh-TW"]` for every entry — observable end-to-end behavior matches the design's "behavior visible after this change ships" section.
- [x] 6.5 [P] Manual smoke in the PWA: with `localStorage["whisper-wrap.locale"] = "en"`, seven English chips render; toggle to 繁中 in settings, page reloads, seven Chinese chips render with no chip blank or rendering the action id — locale toggle round-trip works without code changes between toggles.
