## 1. Backend DELETE endpoint + repo function

- [x] 1.1 [P] Add `delete_action_run(db, session_id, run_id) -> bool` to `app/services/persistence/sessions_repo.py`. Behavior: issues `DELETE FROM action_runs WHERE id = :run_id AND session_id = :session_id`; returns `True` iff rowcount == 1, `False` otherwise (including when the run id exists but belongs to a different session). Verification: new tests in `tests/test_persistence_models.py` exercising happy delete + check absent, wrong-session attempt returns False + row untouched, nonexistent id returns False. `uv run pytest tests/test_persistence_models.py -v` SHALL pass.
- [x] 1.2 [P] Add `DELETE /v1/sessions/{session_id}/runs/{run_id}` route to `app/api/sessions.py`. Behavior: routes to `delete_action_run`; on True returns 204 No Content; on False distinguishes session-missing (404 `{"detail": "session not found"}`) from run-missing (404 `{"detail": "run not found"}`). The route declaration order in the file SHALL not shadow the existing `/audio` subresource or `/{session_id}/finals` / `/{session_id}/runs` routes. Verification: new test cases in `tests/test_sessions_api.py` for happy 204, session-missing 404, run-missing 404, wrong-session-id 404, idempotency (second DELETE returns 404). `uv run pytest tests/test_sessions_api.py -v` SHALL pass.

## 2. Frontend hash routing module

- [x] 2.1 Create `frontend/src/routing/hash-route.ts` exporting `ParsedRoute` type, `parseHash(hash)`, `onRouteChange(handler)` returning unsubscribe, and `navigateToHistory(sessionId?)`. Behavior: `parseHash` is total (never throws) and maps `""` / `"#"` to `{name: "shell"}`, `"#/history"` to `{name: "history", sessionId: null}`, `"#/history/<id>"` to `{name: "history", sessionId: <id>}`, anything else to `{name: "shell"}`; `onRouteChange` registers a `hashchange` listener AND synchronously fires the handler once with the current route. Verification: `frontend/src/routing/hash-route.test.ts` covers each route shape, malformed input ("#/foo", "#/history//", "#/history/abc/def"), and the synchronous initial-fire. `cd frontend && bun run test src/routing/hash-route.test.ts` SHALL pass.

## 3. Frontend HistoryStore + API client deleteRun

- [x] 3.1 [P] Add `deleteActionRun(backendUrl, sessionId, runId): Promise<void>` to `frontend/src/storage/history-api-client.ts`. Behavior: thin `fetch('DELETE ...')` wrapper; on 204 resolves void; on non-204 throws `HistoryApiError` with `status` and parsed `detail`. Verification: extend `frontend/src/storage/history-api-client.test.ts` (or create alongside if absent) with 204 success, 404 throws-with-status, network-error throws cases.
- [x] 3.2 Add `deleteRun(sessionId, runId): Promise<void>` to `HistoryStore` in `frontend/src/storage/history-store.ts`. Behavior: calls `deleteActionRun` from 3.1; on resolve removes the run from the cached session's `action_runs`; on reject fires the configured `onError` callback and re-throws so callers can react; cache SHALL stay untouched on rejection. Verification: extend `frontend/src/storage/history-store.test.ts` with: successful delete prunes cache, 404 leaves cache intact AND fires `onError`, network error leaves cache intact AND fires `onError`. `cd frontend && bun run test src/storage/history-store.test.ts` SHALL pass.

## 4. History view component (master-detail + search)

- [x] 4.1 Create `frontend/src/ui/history-view.ts` exporting `HistoryView` class with the public surface from design Implementation Contract section 2 (`show(sessionId | null)`, `hide()`, `destroy()`). Behavior: mounts a master-detail layout into the given root; rail lists sessions from `store.list()` in `started_at DESC` order; detail panel shows empty state when `sessionId` is null or unknown, otherwise renders meta + transcript body + waveform player + action-runs list + "Add AI Action" control. Clicks on rail rows call `navigateToHistory(<id>)` (route is the source of truth — `show` is only called by the route handler). Verification: `frontend/src/ui/history-view.test.ts` covers empty-rail state, populated-rail rendering, session selection rendering, empty-detail when unknown id passed, action-runs ordered `ran_at DESC`, destroy clears DOM + listeners. `cd frontend && bun run test src/ui/history-view.test.ts` SHALL pass.
- [x] 4.2 Add search input to `HistoryView`. Behavior: text input at the top of the rail; on input, after a 120 ms debounce, the rail visible set updates to sessions whose formatted `started_at` (`YYYY-MM-DD HH:MM`) OR concatenated `finals[].text` contains the query case-insensitively; clearing the input restores all sessions. The debounce SHALL coalesce rapid keystrokes (single render after the user stops typing). Verification: add test cases to `history-view.test.ts` for query filters rail, debounce coalesces, clearing restores, case-insensitive match. Use `vi.useFakeTimers()` + `vi.advanceTimersByTime(120)`.

## 5. Re-run AI Action flow

- [x] 5.1 Add the "Add AI Action" control to `HistoryView`'s detail panel. Behavior: button labeled with i18n key `history.addActionRun`; on click opens the existing action picker (reuse the picker logic from `frontend/src/ui/actions-bar.ts` — if the picker is not extractable as-is, refactor it into `frontend/src/ui/action-picker.ts` consumed by both surfaces). On picker confirm: calls a `runActionAgain(sessionId, actionId, prompt)` callback (provided as a `HistoryView` option), which returns the answer; on resolve, calls `store.appendActionRun` with `{action_id, prompt, answer, ran_at: Date.now()}`; on resolve of that, re-renders the runs list to include the new row at the top. Verification: extend `history-view.test.ts` with: picker invocation, callback receives `(sessionId, actionId, prompt)`, success path appends + re-renders, action-runs remain `ran_at DESC` ordered after the new row arrives.
- [x] 5.2 Add the concurrent re-run guard to "Add AI Action". Behavior: while `runActionAgain` is in flight for the currently selected session, the "Add AI Action" control SHALL be disabled; on resolve OR reject the control SHALL re-enable; the guard SHALL NOT block re-runs for other sessions if the user switches the route mid-flight. Verification: add test case "concurrent re-run guard" — fire the click twice rapidly with a deferred mock promise, assert exactly one `runActionAgain` call and that the button has the disabled attribute between click and resolution.
- [x] 5.3 Wire `runActionAgain` in `frontend/src/main.ts`. Behavior: provide an async function that (a) reads the session's joined transcript from the store, (b) substitutes `{transcript}` in the action template (reuse the same templating helper the actions-bar already uses — extract to a shared module if currently inline), (c) POSTs `{"text": <prompt>}` to `${backendUrl}/ask` and awaits the JSON answer, (d) returns the answer string. Errors propagate so step 5.1's `appendActionRun` is skipped on failure; the existing toast surface handles the `/ask` failure via the standard `onError` path. Verification: extend `frontend/src/main.test.ts` (create if absent) with a mocked fetch for `/ask`; OR cover this via integration in the history-view test by passing a deterministic `runActionAgain` stub and verifying the picker → callback → store call chain.

## 6. Per-run Delete UI

- [x] 6.1 Render a Delete button on each action-run row in `HistoryView`. Behavior: button shows i18n string `history.deleteRunButton`; on click opens a confirm dialog (use the existing `window.confirm` pattern from `history-panel.ts::deleteSession` for consistency, OR a styled inline confirm — implementer chooses); on confirm calls `store.deleteRun(session_id, run_id)`; on resolve removes the row from the panel without a full HistoryView re-render; on reject leaves the row in place (HistoryStore already fires the toast). Verification: add `history-view.test.ts` cases: confirm-and-resolve removes the row, confirm-and-reject leaves the row, cancel does nothing. Use `vi.spyOn(window, 'confirm').mockReturnValue(true|false)`.

## 7. Routing integration + recording-shell ↔ history-view switch

- [x] 7.1 In `frontend/src/main.ts` register `onRouteChange` after boot. Behavior: route name "shell" hides the History view and shows the existing recording shell elements; route name "history" hides the recording shell and calls `historyView.show(sessionId)`; switching between routes SHALL not destroy + recreate the recording shell (its mic state must survive a history visit). The recording shell's "Show history" button SHALL call `navigateToHistory()` instead of toggling the old sidebar. Verification: add or extend a `frontend/src/main.test.ts` integration test using happy-dom: dispatch `hashchange` events, assert visibility of `[data-testid="recording-shell"]` and `[data-testid="history-view"]` containers, assert that mic stop is NOT called when transitioning shell → history. Add the data-testid attributes where missing.

## 8. Styles + i18n

- [x] 8.1 [P] Add master-detail styles to `frontend/src/style.css`: new `.history-view`, `.history-rail`, `.history-detail`, `.history-run` rules; both regions use `display: flex` + `min-height: 0` + `overflow: auto` so each scrolls independently inside the viewport; mobile breakpoint at `max-width: 768px` collapses to single-pane (rail-or-detail toggle as described in spec). Verification: manual — `make build-frontend` succeeds; visiting `#/history` and `#/history/<id>` at 1200 px and 360 px viewports shows expected layouts (verified during step 9 manual sanity).
- [x] 8.2 [P] Add i18n strings to `frontend/src/i18n/strings.ts` for both `en` and `zh-TW`: `history.searchPlaceholder`, `history.empty`, `history.selectPrompt`, `history.addActionRun`, `history.deleteRunConfirm`, `history.deleteRunButton`, `history.runTimestamp`, `history.backToShell`, `history.sessionNotFound`. The keys SHALL be referenced in all `t(...)` calls added in steps 4-7. Verification: existing i18n test (`frontend/src/i18n/strings.test.ts` if present, otherwise `frontend/src/ui/ui-components.test.ts` which uses the strings) SHALL continue to pass; lint SHALL not flag unused keys.

## 9. Manual verification + final lint/typecheck/test pass

- [x] 9.1 Run full backend test suite — `make test` SHALL pass (217 + new test cases). Run `make lint` SHALL pass. Run `cd frontend && bun run test` SHALL pass (existing 38 + new tests). Run `cd frontend && bun run build` SHALL succeed.
- [x] 9.2 Manual sanity: `make dev` then `make build-frontend`; open `http://localhost:<API_PORT>/app/`. Record a short session, navigate to `#/history`, verify the new view loads with the session listed. Click the session, verify transcript + audio player render. Click "Add AI Action", pick a template, verify a new run appears at the top; click Delete on it, verify it disappears. Type a substring of the transcript into the search box, verify the rail filters within ~120 ms; clear it, verify restore. Resize to mobile width (360 px) and verify rail-detail toggle + Back button behavior. Use the browser back button after selecting a session and verify it returns to the rail-only view.
