## 1. Subtitle formatter helper (foundation for `srt` / `vtt` response_format)

- [x] 1.1 Implement SRT and WebVTT formatters in `app/services/subtitle_format.py` exposing `format_srt(segments)` (comma `,` ms separator, blank line between cues, trailing blank line) and `format_vtt(segments)` (`WEBVTT` header + blank line, then `.` ms separator); both accept `list[tuple[float, float, str]]` of `(start_s, end_s, text)`. Verified by `uv run pytest tests/test_subtitle_format.py` covering the two segment example from the spec (`(0.0, 2.5, "hello world.")`, `(2.5, 6.0, " how are you.")`) plus edge cases for zero-duration cues, sub-second timestamps, and the one-hour boundary.

## 2. `OpenAI-compatible audio transcription endpoint` (TDD)

- [x] 2.1 Write failing test `tests/test_openai_compat.py::test_transcribe_default_json` — POST a short wav fixture to `/v1/audio/transcriptions` with `model=whisper-1`, assert HTTP 200, `Content-Type: application/json`, body equals `{"text": "<transcript>"}`. This pins the default scenario from the `OpenAI-compatible audio transcription endpoint` requirement.
- [x] 2.2 Write failing tests for each non-default `response_format` value — `test_transcribe_text_plain` (asserts `Content-Type: text/plain; charset=utf-8` and raw body), `test_transcribe_srt` (asserts body equals the SRT example block from the spec), `test_transcribe_vtt` (asserts `Content-Type: text/vtt; charset=utf-8` and body matches the VTT example block), `test_transcribe_verbose_json` (asserts `task`, `language`, `duration`, `text`, `segments[]` with `tokens=[]`, `avg_logprob=null`, `compression_ratio=null`, `no_speech_prob=null` present per spec).
- [x] 2.3 Write failing tests for the `model` field handling — `test_model_alias_whisper1_silent` and analogues for `gpt-4o-transcribe`, `gpt-4o-mini-transcribe` (no WARNING log emitted via `caplog`), `test_model_unknown_logs_warning` (single WARNING line naming requested and active model), `test_model_empty_returns_400` (asserts OpenAI error envelope with `param="model"`, `type="invalid_request_error"`).
- [x] 2.4 Write failing tests for the OpenAI-shaped error responses — `test_missing_file_400` (`param="file"`), `test_invalid_response_format_400` (`param="response_format"`, message lists accepted values), `test_backend_failure_500_openai_shape` (assert `error.type="server_error"`, message contains no stack trace / file path / secret). Each test asserts the full `{"error": {"message", "type", "param", "code"}}` shape.
- [x] 2.5 Implement `app/api/openai_compat.py` exposing `POST /v1/audio/transcriptions` — multipart form parsing, request-field validation, reuse of `app.state.whisper` for inference, response shaping per `response_format`, `Authorization` header ignored, model alias / WARNING log behaviour. Wire the router into `app/main.py` so the route is mounted at app startup. Verified by `uv run pytest tests/test_openai_compat.py` passing every test from 2.1–2.4 plus `uv run pytest tests/test_main.py` still green.

## 3. `OpenAI-compatible audio translations endpoint` (TDD)

- [x] 3.1 Write failing tests in `tests/test_openai_compat.py` — `test_translate_default_json` (Mandarin clip → body `{"text": "<English translation>"}`, backend invoked with translate task not transcribe), `test_translate_rejects_language_field` (POST with `language=fr` → HTTP 400 OpenAI error envelope with `param="language"`), `test_translate_verbose_json_task_field` (assert `task="translate"` and `language="en"` in the response body). These pin the three scenarios of the `OpenAI-compatible audio translations endpoint` requirement.
- [x] 3.2 Add the `POST /v1/audio/translations` route to `app/api/openai_compat.py` reusing the transcription handler's form parsing / response shaping but rejecting the `language` field and invoking the backend with the translate task. Verified by the three tests in 3.1 passing without regressing the transcription tests from group 2.

## 4. `OpenAI-compatible model discovery endpoint` (TDD)

- [x] 4.1 Write failing tests in `tests/test_openai_compat.py` — `test_models_single_entry` (GET `/v1/models` returns `{"object": "list", "data": [{"id": "<active-model>", "object": "model", "created": <int>, "owned_by": "whisper-wrap"}]}`, `created` equals server startup unix-timestamp), `test_models_modeldir_override` (with `MODEL_DIR` set, `data[0].id` equals the literal string `"<MODEL_DIR>"`, mirroring `/status` `model.name`). These pin the two scenarios of the `OpenAI-compatible model discovery endpoint` requirement.
- [x] 4.2 Add `GET /v1/models` to `app/api/openai_compat.py` sourcing the model name from the same resolution path `app/api/status.py` uses (no separate state) and the timestamp from `app.state` startup time. Verified by the two tests in 4.1 passing.

## 5. `API discovery endpoint lists every registered route` (catalogue update)

- [x] 5.1 Extend `tests/test_status.py` (or `tests/test_api.py`, whichever already covers `GET /`) with `test_discovery_lists_openai_compat_routes` asserting the response `endpoints` array contains entries for `POST /v1/audio/transcriptions`, `POST /v1/audio/translations`, and `GET /v1/models`, each with a non-empty `description`. Confirms the modified `API discovery endpoint lists every registered route` requirement.
- [x] 5.2 Add the three catalogue rows in `app/api/status.py` (or wherever the catalogue is constructed) with concise descriptions matching the spec's example table. Verified by the test in 5.1 passing and `uv run pytest tests/test_status.py tests/test_api.py` staying green.

## 6. Documentation

- [x] 6.1 [P] Add an "open-webui integration" recipe to `docs/INSTALLATION.md` showing the literal command `docker run -p 3000:8080 ghcr.io/open-webui/open-webui:main` and the configuration step pointing open-webui's STT base URL at `http://<host>:8000/v1`. Include a one-paragraph note that whisper-wrap is LAN-only by default so users on multi-machine LANs should bind `API_HOST=0.0.0.0`. Verified by content review confirming the new section is linked from the docs table of contents and the command is copy-pasteable.
- [x] 6.2 [P] Update the "API Endpoints" section in `README.md` to list the three new `/v1/...` routes with a one-line description each plus a link to the `docs/INSTALLATION.md` open-webui recipe. Verified by content review confirming the README's endpoint list matches the spec's discovery payload entries exactly.
- [x] 6.3 [P] Update the `## API Endpoints` section in `CLAUDE.md` to document `/v1/audio/transcriptions`, `/v1/audio/translations`, and `/v1/models` request/response shapes including the five `response_format` values, the model-field aliasing rule, and the OpenAI-shaped error envelope. Verified by content review confirming consistency with `openspec/specs/openai-compat/spec.md`.
