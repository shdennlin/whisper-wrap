# Implementation Tasks — v2.2 Silero-VAD

Tasks are roughly in dependency order. Group 1-2 establish the new module and its tests; Group 3 wires it into the live stream; Group 4 surfaces it through `/status` and config; Group 5 produces the regression fixtures that prove the behaviour change; Group 6 is documentation + manual verification.

## 1. VadBackend protocol module (TDD)

- [x] 1.1 Write failing test `tests/test_vad.py::test_protocol_surface` asserting that `app/services/vad.py` exposes a runtime-checkable `VadBackend` Protocol with an `is_speech(pcm: bytes) -> bool` method, plus `RmsVad` and `SileroVad` classes both conforming to the protocol — covering Decision 1: VadBackend protocol with two implementations + the "Voice activity detection backend is pluggable" requirement; verify the test fails because `app/services/vad.py` does not exist yet.
- [x] 1.2 Implement `app/services/vad.py` exposing the `VadBackend` Protocol + `RmsVad` (lifted from the v2.1 inline `compute_rms` logic) + `SileroVad` (wrapping `silero_vad.load_silero_vad()`); verify by running `tests/test_vad.py::test_protocol_surface` to green and by `python -c "from app.services.vad import VadBackend, RmsVad, SileroVad"`.
- [x] 1.3 Add `silero-vad>=5.0,<6.0` to `pyproject.toml` dependencies (NOT optional — needed for the default Decision 2 path); verify by `uv sync` succeeding and `uv run python -c "import silero_vad"` not raising.

## 2. Factory + auto-fallback (TDD)

- [x] 2.1 Write failing test `tests/test_vad.py::test_factory_returns_silero_when_unset_and_importable` asserting that `make_vad_backend(None)` returns a `SileroVad` instance when `silero_vad` imports successfully — covering Decision 2: Default to silero with auto-fallback to rms; verify the test fails before the factory exists.
- [x] 2.2 Write failing test `tests/test_vad.py::test_factory_falls_back_to_rms_when_silero_missing` asserting that with `import silero_vad` monkey-patched to raise `ImportError`, `make_vad_backend(None)` returns an `RmsVad` instance and emits one INFO log line containing "silero-vad unavailable"; verify the test fails before the fallback logic exists.
- [x] 2.3 Write failing tests `tests/test_vad.py::test_factory_explicit_silero_raises_when_missing` and `tests/test_vad.py::test_factory_explicit_rms_never_imports_silero` and `tests/test_vad.py::test_factory_rejects_unknown_value` covering the remaining Decision 2 scenarios (explicit opt-in with missing pkg, explicit opt-out without import, invalid value rejection); verify each fails before factory logic exists.
- [x] 2.4 Implement `make_vad_backend(name: str | None) -> VadBackend` inside `app/services/vad.py` following the Decision 2 precedence rules; verify by running all four `tests/test_vad.py::test_factory_*` tests to green.

## 3. Stream integration with per-session VAD (TDD)

- [x] 3.1 Write failing test `tests/test_vad.py::test_silero_classifies_fan_noise_as_silence` and `tests/test_vad.py::test_silero_classifies_quiet_speech_as_voice` using mocked silero outputs (deterministic per-chunk speech probabilities) to assert that `SileroVad.is_speech` aggregates across 32 ms sub-chunks of a client frame correctly — covering Decision 4: Run silero-vad on 32 ms frames (512 samples at 16 kHz); verify both fail before frame-chunking logic exists.
- [x] 3.2 Implement the 32 ms sub-chunking inside `SileroVad.is_speech` so each 250 ms client frame is sliced into 512-sample chunks fed sequentially to silero-vad, with any-speech-in-frame mapping to True; verify by running the two failing tests to green.
- [x] 3.3 Write failing test `tests/test_listen.py::test_stream_session_accepts_vad_backend_kwarg` asserting that `StreamSession(transcribe_fn=..., send_event=..., vad_backend=fake_vad)` constructs successfully and uses `fake_vad.is_speech(pcm)` instead of the inline RMS check on every frame — covering Decision 1's "stream.py does not branch on backend" rule; verify the test fails before the refactor.
- [x] 3.4 Refactor `app/services/stream.py` to replace the inline `compute_rms(pcm) >= SILENCE_RMS_THRESHOLD` check with `self.vad_backend.is_speech(pcm)` — covering the "Server applies silence-duration endpointing to finalise utterances" requirement (per-frame VAD decision is delegated to the backend, the surrounding silence-duration accumulator is unchanged), with `vad_backend: VadBackend` as a required keyword-only constructor parameter (default `RmsVad()` for backward test compat); verify by running `tests/test_listen.py::test_stream_session_accepts_vad_backend_kwarg` to green and by `grep "compute_rms\\|SILENCE_RMS_THRESHOLD" app/services/stream.py` returning no matches outside the imports of `RmsVad`.
- [x] 3.5 Write failing test `tests/test_listen.py::test_per_session_vad_state_isolation` constructing two `StreamSession` instances and verifying each calls a distinct `vad_backend` instance produced by the factory — covering Decision 5: Per-session VAD instance, not shared; verify the test fails until the WS handler wiring uses a factory.
- [x] 3.6 Update `app/api/listen.py` to construct each `StreamSession` with a fresh VAD instance via `request.app.state.vad_factory()`, and add `vad_factory: Callable[[], VadBackend]` to the lifespan `app.state` in `app/main.py` — covering the per-session isolation requirement; verify by running `tests/test_listen.py::test_per_session_vad_state_isolation` to green and by manually opening two parallel WS connections (smoke).

## 4. Config + /status integration (TDD)

- [x] 4.1 Write failing test `tests/test_config.py::test_vad_backend_default_is_none` asserting that `Config().VAD_BACKEND` is `None` when the env var is unset (matching the unset-vs-empty-vs-set semantics of `BACKEND_FORMAT`); verify it fails until `app/config.py` adds the field.
- [x] 4.2 Add `VAD_BACKEND: str | None = os.environ.get("VAD_BACKEND") or None` to `app/config.py::Config.__init__`; verify `tests/test_config.py::test_vad_backend_default_is_none` to green plus a companion `test_vad_backend_explicit_silero` that sets the env var to `"silero"` and asserts the field round-trips.
- [x] 4.3 Write failing test `tests/test_status.py::test_status_includes_vad_block_silero` asserting `/status` JSON contains `"vad": {"backend": "silero"}` when the lifespan resolved a `SileroVad` backend — covering the `/status surfaces the active VAD backend` requirement; verify it fails before the response shape change.
- [x] 4.4 Write failing test `tests/test_status.py::test_status_includes_vad_block_rms` asserting `/status` JSON contains `"vad": {"backend": "rms"}` when the lifespan resolved an `RmsVad` backend (either explicit opt-out or auto-fallback); verify it fails until the response shape change is done.
- [x] 4.5 Extend `app/api/status.py` to read the active backend from `request.app.state.vad_factory()().__class__.__name__` (or a more direct `app.state.vad_backend_name: str` set during lifespan) and emit the `vad` block; verify by running both `tests/test_status.py::test_status_includes_vad_block_*` tests to green.

## 5. Regression fixtures + behaviour tests (TDD)

- [x] 5.1 Capture three PCM fixtures committed to the repo (Decision 6: Test fixtures are recorded PCM clips, not synthesised noise) at `tests/fixtures/vad/clean_speech.pcm`, `tests/fixtures/vad/fan_noise.pcm`, and `tests/fixtures/vad/quiet_speech.pcm` — each exactly 5 s of 16 kHz mono pcm_s16le (160000 bytes). Clean speech: macOS `say -v Meijia '今天天氣很好，台北的下午總是這麼舒服。' -o /tmp/clean.aiff` then ffmpeg-convert. Fan noise: `ffmpeg -f lavfi -i 'anoisesrc=duration=5:color=brown:amplitude=0.15' -ar 16000 -ac 1 -f s16le tests/fixtures/vad/fan_noise.pcm`. Quiet speech: same `say` command piped through `ffmpeg -af "volume=0.15"`. Verify each file is exactly 160000 bytes via `stat -f%z` and audible via `ffplay`.
- [x] 5.2 Write `tests/test_listen.py::test_fan_noise_emits_zero_events_with_silero` replaying `fan_noise.pcm` through a `StreamSession` constructed with `SileroVad`, asserting zero `partial` events and zero `final` events — covering the "Silero-vad correctly endpoints continuous-noise environments" requirement; verify it green-bars (regression target).
- [x] 5.3 Write `tests/test_listen.py::test_quiet_speech_captures_utterance_with_silero` replaying `quiet_speech.pcm` through a `StreamSession` constructed with `SileroVad` + a fake transcribe_fn returning constant text, asserting at least one `partial` event and exactly one `final` event — covering the "Silero-vad captures quiet speech the RMS threshold misses" requirement; verify it green-bars.
- [x] 5.4 Write parametrised regression test `tests/test_listen.py::test_rms_baseline_documents_failure_modes` that runs `fan_noise.pcm` and `quiet_speech.pcm` through a `StreamSession` constructed with `RmsVad`, asserting the v2.1 failure modes (fan-noise emits ≥1 partial, quiet-speech emits zero events); this test SHALL be marked `@pytest.mark.skip(reason="Documents v2.1 failure mode silero-vad fixes")` so it stays green but does not break the suite — it serves as executable documentation that we know the failure exists.

## 6. Documentation + manual verification

- [x] 6.1 [P] Update `.env.example` to document `VAD_BACKEND` alongside `BACKEND_FORMAT` (commented out by default, mentioning the silero/rms accepted values and the auto-fallback behaviour); verify by content review against the `VAD_BACKEND env var selects the active backend` requirement.
- [x] 6.2 [P] Update `README.md` Features section to mention "neural VAD via silero-vad (RMS fallback)" and `CLAUDE.md` Configuration block to list `VAD_BACKEND` alongside `BACKEND_FORMAT`; verify by content review.
- [x] 6.3 [P] Update `docs/INSTALLATION.md` with a "silero-vad model cache" subsection — covering Decision 3: TorchScript model lives in the standard PyTorch hub cache explaining the one-time TorchScript download to `~/.cache/torch/hub/snakers4_silero-vad/`, the air-gapped warning, and the `make setup` priming step that fetches the model proactively; verify by content review.
- [x] 6.4 Manual verification on the developer's Mac mini: create `openspec/changes/v2-2-silero-vad/verification-notes.md` with two H2 sections — `## Fan noise rejection` (run `scripts/bench-stream-latency.py` against `tests/fixtures/vad/fan_noise.pcm`, confirm zero partial/final events with silero, capture before/after event counts) and `## Quiet speech capture` (record a quiet Mandarin utterance through `scripts/live-caption.py`, confirm one `final` event captures the speech with silero, contrast with rms missing it); verify by content review.
- [x] 6.5 Run `make test && make lint` on macOS with silero-vad installed and `VAD_BACKEND` unset; verify all tests pass (the v2.1 baseline of 190 tests grows to ≥200) and ruff is clean — this satisfies the v2.2 Phase done criterion analogous to v2.1's 11.4.
