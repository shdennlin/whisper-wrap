## 1. Storage foundation

- [x] 1.1 Build audio-store with TDD: write failing vitest cases in `frontend/src/storage/audio-store.test.ts` for put / get / delete / clear / totalBytes round-trips against the **IndexedDB schema** (object store + `by_stored_at` index) and for the **Eviction policy: FIFO by `stored_at` until under budget** (oldest records deleted first AND the matching `whisper-wrap.sessions` entry survives eviction); then implement `frontend/src/storage/audio-store.ts` until `cd frontend && bun run test src/storage/audio-store.test.ts` is green. Satisfies the **Per-session audio persistence in IndexedDB** and **User-configurable byte budget with FIFO eviction** spec requirements.

## 2. Independent module builds (parallel-safe TDD)

- [x] 2.1 [P] Build dual-recorder with TDD covering the **Storage format: `audio/webm;codecs=opus` (with `audio/mp4` Safari fallback)** decision: write failing vitest in `frontend/src/capture/dual-recorder.test.ts` asserting MIME selection priority (`audio/webm;codecs=opus` first, then `audio/mp4`), Batch mode returns a non-null blob with the chosen MIME, Live mode resolves with a parallel-recorder blob alongside the PCM tap, and `saveAudio = false` resolves with `blob: null`; then implement `frontend/src/capture/dual-recorder.ts` to make the suite green.
- [x] 2.2 [P] Build waveform-peaks with TDD per the **Waveform rendering: peaks-on-canvas, computed once and cached** decision: write failing vitest in `frontend/src/ui/waveform-peaks.test.ts` asserting `computePeaks(samples, columns)` returns one `[min, max]` pair per column for known Float32Array inputs (including a constant-zero array and a sine-wave fixture); then implement `frontend/src/ui/waveform-peaks.ts` to make the suite green.
- [x] 2.3 [P] Build WaveformPlayer with TDD covering the **Player state machine** decision and the **Waveform player in every history card** spec requirement: write failing vitest in `frontend/src/ui/waveform-player.test.ts` asserting transitions `loading → ready → playing → paused`, click-to-seek arithmetic `(clickX / canvas.width) * duration`, `expired` and `missing` state rendering with disabled controls and the correct i18n labels, and that an `AudioContext.decodeAudioData` rejection transitions the player to `error` with the play button disabled; then implement `frontend/src/ui/waveform-player.ts` to make the suite green.
- [x] 2.4 [P] Build ReAsrForm with TDD per the **Re-ASR flow** decision and the **Re-transcribe action posts stored blob to /transcribe** spec requirement: write failing vitest in `frontend/src/ui/re-asr-form.test.ts` asserting the success path POSTs `multipart/form-data` to `/transcribe` with the supplied prompt + language and appends one `ActionRun` with `action_id = "re_asr"`, and the failure path renders the inline `audio.reTranscribeFailed` error without touching `action_runs`; then implement `frontend/src/ui/re-asr-form.ts` to make the suite green.
- [x] 2.5 [P] Build Settings additions with TDD per the **Settings additions** decision and the **Settings controls for opt-out, budget, and clear-all** spec requirement: extend `frontend/src/ui/actions-and-settings.test.ts` with failing cases asserting the `audio.save` toggle defaults to `true`, the `audio.budget_mb` numeric field rejects values outside `[10, 1000]` with an inline error, and the clear-all button only acts after a double-confirm; then implement the new Settings fields in `frontend/src/ui/settings-panel.ts` to make the suite green.

## 3. Integration wiring + i18n

- [x] 3.1 Wire dual-recorder into the capture pipeline per the **Capture wiring** decision: update `frontend/src/capture/mic-pipeline.ts` to construct a `DualRecorder` for both modes and update `frontend/src/main.ts` so each session-stop writes the resolved blob (with the `mime_type` and `duration_ms` from the recorder) into `AudioStore`. Verified manually by recording one Batch session and one Live session and confirming each leaves exactly one record in the `audio` object store (use DevTools → Application → IndexedDB), and by toggling Settings `audio.save` off and confirming no record is written for the next recording.
- [x] 3.2 Mount `WaveformPlayer` in every `HistoryPanel` card (passing the stored audio record, or `{kind: "missing"}` / `{kind: "expired"}` when absent), and render the Re-transcribe button only when the session has a stored audio record; wire the Settings clear-all button to `AudioStore.clear()` and emit the `settings.audioClearedToast` toast with the returned count. Verified manually: every expanded history card renders a waveform whose play / pause / seek behave per the spec scenarios, pre-capability and evicted sessions render the disabled labelled state, the Re-transcribe button is hidden for sessions without audio, and clear-all empties IndexedDB while the transcript cards remain.
- [x] 3.3 Add the new **i18n keys** under `audio.*` and `settings.audio*` to `frontend/src/i18n/strings.ts` for both `en` and `zh-TW` covering the `audio.playerExpired`, `audio.playerNoAudio`, `audio.playerLoading`, `audio.playerError`, `audio.reTranscribe`, `audio.reTranscribeSubmit`, `audio.reTranscribeCancel`, `audio.reTranscribePromptLabel`, `audio.reTranscribeLanguageLabel`, `audio.reTranscribeFailed`, `audio.evicted`, `settings.audioSaveLabel`, `settings.audioSaveHint`, `settings.audioBudgetLabel`, `settings.audioBudgetHint`, `settings.audioClearAllButton`, `settings.audioClearAllConfirm`, and `settings.audioClearedToast` strings; verified by `cd frontend && bun run test src/i18n/index.test.ts` (the existing locale-key-parity case must still pass after the additions).

## 4. Final verification

- [x] 4.1 Run `cd frontend && bun run test` and confirm every suite is green and the total test count is at least `95` (up from the current `71`).
- [x] 4.2 Run `make build-frontend` and confirm the build succeeds and the gzipped JS bundle size has increased by less than `+10 KB` versus the pre-change baseline (record the before / after numbers in the verification output). Then run the end-to-end manual smoke test: record one Batch session and one Live session with `audio.save = true`, confirm each card renders a working waveform player with play / pause / click-to-seek; press Re-transcribe on each, confirm a new `re_asr` entry appears in the card's AI-response section; lower `audio.budget_mb` to `10`, record additional sessions until eviction fires, confirm the eviction toast appears and the oldest cards now render the `audio.playerExpired` state with their transcript text intact; toggle `audio.save = false` and record one more session, confirm the new card renders `audio.playerNoAudio`; press clear-all (with double-confirm), confirm IndexedDB is empty and the toast names the deleted count.
