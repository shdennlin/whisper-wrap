## 1. Backend loader — category field on actions (Action entries SHALL support an optional category field with bilingual display label)

- [x] 1.1 Extend `ActionTemplate` in `app/services/actions.py` with optional `category: str | None` and `category_labels: Mapping[str, str] | None` fields so that **action entries SHALL support an optional category field with bilingual display label** is satisfied — verify with pytest `tests/test_actions.py::test_action_template_exposes_category_fields` asserting both attributes exist with `None` defaults for entries without a `category`.
- [x] 1.2 [P] Normalise string-form `category: cleanup` shorthand to `(id="cleanup", labels=None)` so existing flat-string configs keep working — verify with pytest `tests/test_actions.py::test_string_category_normalises_to_id_only` asserting `action.category == "cleanup"` and `action.category_labels is None`.
- [x] 1.3 [P] Load mapping-form `category: {id: cleanup, labels: {en: Cleanup, "zh-TW": 清理}}` verbatim into `category` id plus bilingual `category_labels` — verify with pytest `tests/test_actions.py::test_mapping_category_loaded_verbatim` asserting both fields per the spec scenario.
- [x] 1.4 Raise `ActionRegistryError` naming the offending action id when `category` is neither a string nor a mapping (e.g., `42`, `[]`) so the server refuses to start on a broken file — verify with pytest `tests/test_actions.py::test_invalid_category_type_refuses_startup` parameterised over int and list.
- [x] 1.5 [P] Raise `ActionRegistryError` when a mapping `category` is missing `id` or has an empty / non-string-valued `labels` sub-mapping — verify with pytest `tests/test_actions.py::test_mapping_category_validation_refuses_startup` covering missing `id` and empty `labels`.
- [x] 1.6 [P] Emit a WARNING (not error) when an action's `category` id is not declared in the top-level `categories:` block, AND still pass the entry through so the API exposes the unknown id verbatim — verify with pytest `tests/test_actions.py::test_unknown_category_id_warns_and_passes_through` using `caplog` to assert one WARNING with both action id and category id.

## 2. Backend loader — top-level categories block (Registry SHALL declare category display order and bilingual labels)

- [x] 2.1 Add a `CategoryDefinition` dataclass and `load_categories(path)` helper to `app/services/actions.py` that returns an ordered list of `(id, label, labels)` from the top-level `categories:` block — verify with pytest `tests/test_actions.py::test_load_categories_returns_ordered_definitions` covering a four-entry block and asserting iteration order matches YAML order.
- [x] 2.2 [P] Raise `ActionRegistryError` when the `categories:` block has a duplicate `id` so the registry SHALL declare category display order and bilingual labels without ambiguity — verify with pytest `tests/test_actions.py::test_duplicate_category_id_refuses_startup` asserting the duplicate id is named in the error message.
- [x] 2.3 [P] Raise `ActionRegistryError` when a `categories:` entry is missing `id`, has an empty `id`, or has a label that fails the existing string-or-mapping rules (empty mapping, non-string value, neither type) — verify with pytest `tests/test_actions.py::test_invalid_category_definition_refuses_startup` parameterised over those failure modes.
- [x] 2.4 [P] Return an empty list from `load_categories(path)` when the top-level `categories:` block is absent or `[]`, without raising — verify with pytest `tests/test_actions.py::test_missing_categories_block_returns_empty_list` asserting both shapes produce `[]`.

## 3. API — surface category fields in GET /actions response (GET /actions SHALL surface category information for client-side grouping)

- [x] 3.1 Update the response so GET /actions SHALL surface category information for client-side grouping, including both `category` (resolved id string OR `null`) and `categoryLabels` (bilingual mapping OR `null`) per entry, keeping the existing `id`/`label`/`labels`/`template` fields unchanged — verify with pytest `tests/test_actions.py::test_get_actions_includes_per_entry_category_fields` asserting both keys exist on every entry of the shipped registry response.
- [x] 3.2 Add a top-level `categories: [{id, label, labels}]` array to the `GET /actions` response, ordered to mirror the YAML `categories:` block declaration order — verify with pytest `tests/test_actions.py::test_get_actions_includes_top_level_categories` asserting the shipped response's `body["categories"]` ids equal `["raw", "cleanup", "structure", "transform"]` in that exact order.
- [x] 3.3 [P] Confirm clients that ignore the new keys still receive a backward-compatible body — verify with pytest `tests/test_actions.py::test_get_actions_legacy_keys_still_present` asserting every entry retains the original `{id, label, labels, template}` set even when `category` is added.

## 4. Registry rewrite — four categories plus seventeen chips (Shipped registry SHALL include ten additional chips across the four categories)

- [x] 4.1 Rewrite `registry/actions.yaml` to add the top-level `categories:` block declaring `raw`, `cleanup`, `structure`, `transform` in that order with bilingual labels (`en` + `zh-TW`) — verify with pytest `tests/test_actions.py::test_shipped_registry_declares_four_categories` asserting `[c.id for c in load_categories(DEFAULT_REGISTRY_PATH)] == ["raw", "cleanup", "structure", "transform"]` and every entry has both locale labels.
- [x] 4.2 [P] Assign the existing seven chips (`passthrough`, `cleanup-light`, `punctuate`, `polish`, `meeting-notes`, `translate-en`, `formalize`) to their categories per the spec mapping (raw / cleanup×3 / structure×1 / transform×2) without altering existing templates or label content — verify with pytest `tests/test_actions.py::test_existing_chips_keep_ids_and_templates` asserting templates byte-equal the previous registry content while `category` is now populated.
- [x] 4.3 Add the new `fix-only-asr` chip under the `cleanup` category with a template that preserves fillers and ASR-character fixes only — verify with pytest `tests/test_actions.py::test_fix_only_asr_contract` asserting the template contains the phrases "ASR" and "PRESERVE fillers" (case-insensitive) and `{transcript}`.
- [x] 4.4 [P] Add seven new `structure` chips — `summary-tldr`, `bullet-outline`, `extract-todos`, `questions-raised`, `code-spec`, `1on1-notes`, `standup-recap` — each with bilingual labels and templates matching the per-chip contracts listed in the spec — verify with pytest `tests/test_actions.py::test_structure_chips_have_required_headings_or_format` parameterised over each new structure chip's expected output marker (`[NEEDS CLARIFICATION]` for code-spec/1on1-notes/standup-recap, numbered list for questions-raised, `- [ ]` for extract-todos, "TL;DR"-like phrase or word-target wording for summary-tldr, `## Yesterday` for standup-recap).
- [x] 4.5 [P] Add the two new `transform` chips — `translate-zh` and `email-draft` — with bilingual labels and templates: `translate-zh` mirrors `translate-en` translating to zh-TW verbatim; `email-draft` outputs `Subject: ...` followed by greeting/body/signoff — verify with pytest `tests/test_actions.py::test_transform_new_chips_contracts` asserting `translate-zh` template references "Chinese" or "zh-TW" and `email-draft` template references "Subject:" plus "signoff" or "closing".
- [x] 4.6 Update the shipped-list assertion to expect seventeen chips in the category-grouped declared order — this completes the Shipped registry SHALL include ten additional chips across the four categories contract — verify by replacing the existing `SEVEN_BUILTIN_IDS` test with `tests/test_actions.py::test_shipped_registry_contains_seventeen_chips_in_order` asserting `[a.id for a in load_actions(DEFAULT_REGISTRY_PATH)] == ["passthrough", "fix-only-asr", "cleanup-light", "punctuate", "polish", "meeting-notes", "summary-tldr", "bullet-outline", "extract-todos", "questions-raised", "code-spec", "1on1-notes", "standup-recap", "translate-en", "translate-zh", "formalize", "email-draft"]`.

## 5. Frontend — group chips by category with Misc fallback (PWA chip bar SHALL render chips grouped by category in the declared order)

- [x] 5.1 Extend the TypeScript `ActionTemplate` interface and add a `Category` interface in `frontend/src/ui/actions-bar.ts` to accept the new `category`, `categoryLabels`, and top-level `categories` fields from `GET /actions` — verify with vitest `frontend/src/ui/actions-and-settings.test.ts::"interfaces accept new category fields"` (type-only assertion using `satisfies` or fixture object) asserting the shapes compile against a mocked seventeen-chip response.
- [x] 5.2 Update `ActionsBar.fetchActions` callsite in `frontend/src/main.ts` to also surface `body.categories ?? []` into the bar's render context so the PWA chip bar SHALL render chips grouped by category in the declared order — verify with vitest `frontend/src/ui/actions-and-settings.test.ts::"renders four category headings in declared order"` mocking the full response and asserting the chip bar contains four `.actions-category-heading` elements whose text equals the active-locale label of `raw`/`cleanup`/`structure`/`transform`.
- [x] 5.3 [P] Implement category grouping so chips with `category: null` or with a `category` id not present in the top-level `categories` array render under a trailing Misc group whose heading is `t("actions.miscCategoryLabel")` — verify with vitest `frontend/src/ui/actions-and-settings.test.ts::"unknown category falls into Misc bucket"` mocking one chip with `category: "experimental"` plus three known-category chips and asserting Misc heading appears last with exactly the experimental chip beneath.
- [x] 5.4 [P] Add the `actions.miscCategoryLabel` key to `frontend/src/i18n/strings.ts` for `en` (`"Misc"`) and `zh-TW` (`"其他"`) — verify with vitest `frontend/src/i18n/index.test.ts::"miscCategoryLabel localises"` asserting `t("actions.miscCategoryLabel")` returns `"Misc"` after `saveLocale("en")` and `"其他"` after `saveLocale("zh-TW")`.
- [x] 5.5 [P] Add minimal styling for `.actions-category-heading` and a group wrapper in `frontend/src/style.css` so each category renders as a small uppercase-or-medium heading above its chips with vertical spacing between groups, keeping mobile single-column layout — verify by manual visual check during task 6.4 and by vitest `frontend/src/ui/actions-and-settings.test.ts::"category groups have distinct DOM containers"` asserting each `.actions-category-heading` is followed by its own `.actions-category-chips` container.
- [x] 5.6 [P] Preserve the empty-registry fallback so when `fetchActions` throws or returns an empty list the bar STILL renders one `passthrough` chip with NO category heading — verify with vitest `frontend/src/ui/actions-and-settings.test.ts::"fallback path renders no category headings"` asserting zero `.actions-category-heading` elements after a 502 fetch.

## 6. Verification

- [x] 6.1 Run `uv run pytest tests/test_actions.py -v` and confirm every test from sections 1–4 passes — pytest action-registry suite is green.
- [x] 6.2 [P] Run `cd frontend && bun run test` and confirm every test from section 5 passes plus the existing 74 tests stay green — vitest suite is green with no regressions.
- [x] 6.3 [P] Run `make lint` and confirm no new ruff warnings from the loader changes — lint is clean.
- [x] 6.4 Manual smoke: start the server with the new YAML, `curl -s $HOST/actions | jq '.actions | length'` returns `17`, `jq '.categories | map(.id)'` returns `["raw","cleanup","structure","transform"]`, and `jq '.actions[].category'` returns no `null` values — backend end-to-end matches the design contract.
- [x] 6.5 [P] Manual smoke in the PWA via Chrome with `localStorage["whisper-wrap.locale"] = "en"`: four headings render (`Raw`, `Cleanup`, `Structure`, `Transform`) above the seventeen chips in the documented order; toggle to `"zh-TW"` + reload: headings become `原文`/`清理`/`結構化`/`轉換` and chip labels switch to Chinese — locale toggle on grouped chip bar works.
